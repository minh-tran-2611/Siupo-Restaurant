server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # gzip cho asset tĩnh
    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # SPA: mọi route không khớp file -> index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}

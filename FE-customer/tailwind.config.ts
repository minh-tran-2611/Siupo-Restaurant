import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        miniver: ["Miniver", "cursive"],
      },
    },
  },
  plugins: [],
};

export default config;
